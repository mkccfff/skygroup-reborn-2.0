# СКАЙГРУПП — HANDOFF для нового чата

> Этот файл = полное состояние проекта + что осталось сделать. Читать первым.
> Дата: июнь 2026. Предыдущий чат упёрся в лимит.

---

## 0. Быстрый старт

- **Проект (актуальная версия):** `D:\claude\skygroup-reborn-2.0` — React + Vite + Tailwind + framer-motion + Lenis.
- **Один большой файл:** почти всё в `src/App.tsx` (~2100 строк). Ещё: `src/components/site/CrimeaMap.tsx` (карта), `src/components/site/FloatingPaths.tsx` (сейчас НЕ используется — см. §6), `src/index.css` (утилиты: `.grad-text`, `.font-num`, `.glass`, `.bg-grid`, `.animate-marquee`).
- **Запуск превью:** launch.json конфиг `skygroup-reborn-2.0`, порт **5275**, открывать `http://localhost:5275`. `node_modules` — junction на `skygroup-future\node_modules`.
- **Если правки «не видны»** на localhost — **Ctrl+F5** (кэш). Если порт завис: `Get-NetTCPConnection -LocalPort 5275 -State Listen | %{ Stop-Process -Id $_.OwningProcess -Force }`, потом перезапустить превью.
- **Бренд:** blue `#0F4C81`, teal `#185B69`, ink `#0D1117`. Шрифты: Syne (заголовки `font-display`), Manrope (текст `font-sans` + цифры `.font-num`). **Бренд оставляем КИРИЛЛИЦЕЙ «СКАЙГРУПП»** (не латиницей — решено).

## 1. Деплой (GitHub Pages)

- **Живой сайт: https://mkccfff.github.io/skygroup/** — аккаунт `mkccfff`, репо `mkccfff/skygroup` (public), ветка `main`, root.
- ⚠️ **Живой сайт со СТАРОЙ версией** (батч 1). Все батчи 2–5 только в dev. Редеплой нужен новый PAT (старый отозван).
- **Сборка под Pages:** `node node_modules/vite/bin/vite.js build . --base=./` (пути к ассетам в коде сделаны ОТНОСИТЕЛЬНЫМИ `assets/...`, не `/assets/`). dist — это свой git-репо + `.nojekyll`. Перед сборкой убрать тяжёлые временные файлы из `public/`.
- **Редеплой:** собрать → `git -C dist add -A; commit; push https://<TOKEN>@github.com/mkccfff/skygroup.git main --force` → токен из конфига чистить.

## 2. Структура секций (App.tsx, порядок = «экраны») — ОБНОВЛЕНО (батч 7)

`Hero`(1) → `DeveloperIntroPremium`(2, ТЁМНЫЙ рендер-фон g2) → `Projects`(3) → `AboutAdvantages`(4) → `OurAwards`(5, **награды**, тёмный рендер-фон g1, компактный) → `Metrics`(6) → `MapSection`/CrimeaMap(7) → `GalleryFullscreen`(8, полноэкранный слайдер) → `DirectorSectionPremium`(9) → `Team`(10) → `News`(11) → `CTASection`(12) → `Footer`.

> ⚠️ Блок «Инфраструктура и сервис» (`InfrastructureByObject`) УДАЛЁН (батч 7) — инфа из буклета (`OBJECTS[].stats`) перенесена в **`ObjectModal`** (общий поп-ап карты и кнопки «Открыть проект»). Нижний блок наград тоже удалён ранее. Мёртвый код (`Process`/`Infrastructure`/`DeveloperIntro`/`Marquee`/`INFRA`/`PROCESS`/старый `GALLERY`) остался — вынесен в отдельную задачу на чистку.

> 🎨 **Рендеры не повторяются (батч 7):** блок 2 = `g2`; награды-фон = `g1`; галерея = `g5,g3,g4,g8,g7`; проекты+поп-апы = `skysoul/santerra/ptica/olympia/meddirt`. `g6` (двойник skysoul) НЕ используется. News-миниатюры показывают свой проект (santerra/skysoul/meddirt) — по контексту.

---

## 3. ⭐ КОРРЕКТНЫЕ ДАННЫЕ (из буклета + дашборда — добыто рендером PDF, текстом не извлекается!)

### Корпоративные цифры SKYGROUP (буклет, стр.1) — УЖЕ внесены в `DEV_STATS`
- **397 000+ м²** введено в эксплуатацию
- **330 000+ м²** на этапе строительства
- **295+ ГА** земельный банк
- **14** масштабных проектов
- **13 400+** рабочих мест (в DEV_STATS не вошло, можно добавить)
- Бренды: SKYGROUP DEVELOPMENT, SKYSOUL HOTEL GROUP, УК «СКАЙСОУЛ», генподряд «Интеграл». Сайты: `ukskysoul.ru`, `skygrouphotel.ru`.
- Реальная цитата основателя (Шацких): **«Мы создаём пространства, которые становятся точками притяжения для миллионов»** (уже внесена в блок директора).

### SKYSOUL (буклет стр.3 + дашборд `skysoul_dashboard.pdf`)
- 14,5 ГА · 85 000+ м² застройки · **2 500 номеров** · I очередь введена **2025** · Коктебель, вид на Кара-Даг. (Внесено в OBJECTS.)
- **Дашборд роста цены (3 очереди):**
  | | 1 оч | 2 оч | 3 оч |
  |---|---|---|---|
  | Старт продаж | II'2022 | II'2023 | II'2024 |
  | Цена м² старт | 180 000 | 223 000 | 255 000 |
  | Цена м² сейчас | **604 696** | **472 251** | **427 227** |
  | Рост | +236% | +136% | +80% |
  | Ср. рост/год | ~59% | ~45% | ~40% |
  | Прогноз м² II'2027 | 660–680к | 520–550к | 470–510к |
  | MIN цена объекта | 20 213 364 | 11 355 474 | 13 327 187 |
  | Рост MIN | +302% | +125% | +95% |
  | Распродано | 90% (600/668) | 76% (604/794) | 62% (403/654) |
  - На сайте цена SKYSOUL обновлена → **«от 427 000 ₽/м²»** (минимальная текущая = 3 оч).

### ПТИЦА (буклет стр.5) — внесено в OBJECTS
- Санаторно-курортный «**Санаторий XXI века**», первая береговая линия: 10 ГА · **1 686 номеров** · 30 000 м² пляж · 700 м² бассейн (морская вода) · 1 960 м² СПА · артезианская скважина 1 200 м. Собственный санаторный корпус + медицина.

### ❌ НЕ извлечено (буклет тяжёлый, 58 МБ, 12 стр.) — НУЖНО добить
- **САНТЕРРА, ОЛИМПИЯ (Жигулина Роща), МЕДДИРТ** — точные цифры (м²/номера). На сайте пока старые значения (помечены). Как читать буклет — см. §6.

---

## 4. ✅ ЧТО УЖЕ СДЕЛАНО (батчи 1–7)

**Батч 7 (последний — правки по фидбэку коллег):**
- **Блок 2 Девелопер** — обратно на ТЁМНЫЙ рендер-фон (юзер: «уходим от белого, рендеры на фон»). Рендер `g2` (аэро, не дублируется). Цифры-счётчики БОЛЬШЕ НЕ НАЕЗЖАЮТ: размер ограничен `clamp(30px,4.2vw,68px)` (на 1440 = 60px, «397 000»=236px в колонке 300px; на 375 = 30px в колонке 152px). «для Крыма» — solid `text-sky-300` (читаемо на тёмном).
- **Награды** — переделан в стилистику сайта: ТЁМНЫЙ рендер-фон `g1` (синий час), КОМПАКТНЕЕ (py-16/20), бейджи белыми карточками в один ряд (5 шт, `lg:grid-cols-5`). Убран большой диплом-фичер и счётчик.
- **Блок «Инфраструктура»** УДАЛЁН. Инфа из буклета (все `OBJECTS[].stats`, 4–6 на объект) перенесена в **`ObjectModal`** — теперь поп-апы карты И кнопки «Открыть проект» показывают «Инфраструктура и преимущества». (Один компонент → оба поп-апа.)
- **Галерея** — оставлены только УНИКАЛЬНЫЕ рендеры `g5,g3,g4,g8,g7` (нет в других секциях). `g6` (двойник skysoul) убран.
- Цель «рендеры не повторяются» выполнена для всех крупных/фоновых рендеров (см. §2).
- ⚠️ Проверка снова замером DOM (вкладка превью скрыта → rAF/IO заморожены; счётчики читаются как «0», но это артефакт — реальную ширину цифр мерил, подставляя финальные значения).

**Батч 6 (большой пакет правок):**
- **Данные объектов (OBJECTS)** — все описания переписаны СТРОГО из буклета (Skysoul/Птица/Сантерра/Меддирт/Олимпия). Цифры из буклета в новом поле `stats[]` (value+label). Цена: только SKYSOUL «от 427 000 ₽/м²», остальные — **«по запросу»** (по указанию юзера). ObjectModal теперь показывает первые 4 `stats` вместо area/units (поля area/units удалены из интерфейса).
- **Экран 1 Hero** — подключён новый ролик юзера (пролёт над побережьем, комплекс «вырастает»). Перекодирован 4K→1080p: `hero.mp4` (48МБ→**4МБ**), постер `hero.jpg` с 1-го кадра. Оригинал 4K в `_media-backups/hero-prev3-drone.mp4`.
- **Экран 2 Developer** — переделан в **СВЕТЛЫЙ** блок (чтобы наш синий читался): радужный градиент на «для Крыма» → `text-brand-blue`, **черточки убраны**, цифры-счётчики КРУПНЕЕ (clamp до 88px) и брендовым синим. Рендер фона `skysoul.jpg` → **`gallery/g5.jpg`** (не дублируется в Projects ниже).
- **Экран 5** — `ProcessPremium` («Всё в одних руках») заменён на **`OurAwards`** (блок «Наши награды») с реальными бейджами из `public/assets/awards/`: URBAN победитель 2024/2025, финалист 2024, Рекорды рынка, диплом Crimea Urban Awards (ПТИЦА). Счётчик «5 наград».
- **Экран 6** — `InfrastructureSlider` заменён на **`InfrastructureByObject`**: табы 5 объектов → рендер + преимущества/цифры из буклета (`OBJECTS[i].stats`).
- **Экран 9** — `GalleryCompact` (masonry+lightbox) заменён на **`GalleryFullscreen`**: каждый рендер на весь экран (88vh), свайп (framer drag) + стрелки + клавиши + миниатюры (десктоп). Рендеры переупорядочены (лучшие вперёд).
- **Экран 10 Director** — правая панель из тёмно-серой стала **СВЕТЛОЙ** (наш синий читается): градиент на «точками притяжения для миллионов» → `text-brand-blue`, **черточки убраны**, цифры крупнее (clamp до 46px) синим.
- **Экран 13→12 CTA** — заголовок «Начните историю своего наследия» → **«Премиальный Крым для жизни и инвестиций»** (не как Олимпия). Текст «Дом у моря…» → про весь портфель (курорты/резиденции/кварталы, не только дом у моря).
- **Новости** — добавлена кнопка **«Все новости»** + карточки кликабельны → ведут на блог Тильды `https://xn--80agrc2afcv.xn--p1ai/blog`. (Тильда рендерится JS — текст новостей не скрейпится, поэтому только ссылка-переход.)
- Удалены неиспользуемые импорты иконок (Landmark/Ruler/Trophy/Medal/Star/Award/Quote).
- ⚠️ **Проверка**: вкладка превью была СКРЫТА (`document.hidden=true`) → в ней заморожены rAF/IntersectionObserver, поэтому скриншоты таймаутят, счётчики/Reveal стоят на 0/opacity:0. Проверено **замером DOM** (цвета, данные, клики табов, переключение галереи, отсутствие overflow на 375px) — всё корректно. В реальном браузере юзера анимации идут.

**Батч 5 (правки по 14 экранам):**
- Хедер: «Связаться» — полупрозрачная (стекло).
- Экран 1 Hero: текст обновлён («премиум, бизнес и элит-класса недвижимость у моря»); «Смотреть проекты» — полупрозрачная. Видео стройки — **юзер делает сам** (другая нейронка).
- Экран 2 Developer: **полная переделка** — рендер SKYSOUL на фоне, крупные белые цифры из буклета.
- Экран 3 Projects: тексты всех объектов переписаны; SKYSOUL/ПТИЦА цифры из буклета. Сантерра/Олимпия/Меддирт — старые цифры (флаг).
- Экран 4 AboutAdvantages: заголовок «Полный цикл девелопмента», 4 карточки (генподряд/отельный оператор/УК/санатории), 5–6 убраны. Высота 320vh, ABOUT_POS/RANGES под 4 станции.
- Экран 10 Director: убраны «черточки», тёмно-серый фон (#30343c, продолжение фото), белый текст, цитата из буклета, цифры 14/397000/295.
- Экран 11 Team: фото крупнее (`lg:grid-cols-3`).
- Экран 12 Awards: убрана анимация «черточек».
- Экран 13 News: заголовок «Новости СКАЙГРУПП девелопмент».
- Экран 14 CTA: «Начните историю своего наследия» (про наследие).
- DEV_STATS обновлены из буклета.

**Батчи 1–4 (ранее):** карта Крыма full-bleed с точками (Меддирт+Птица в Саках), ProcessPremium (интерактивные этапы), InfrastructureSlider (карусель), DirectorSectionPremium (editorial-split), Awards-блок, hero-видео, about-видео 360° (all-intra 2160p, scroll-scrub), DEV_STATS без обрезки, шрифт цифр Manrope, мобильная адаптация всех блоков, фикс багов.

---

## 5. ⏳ ЧТО ОСТАЛОСЬ + БЛОКЕРЫ (приоритет нового чата)

### Нужно уточнение направления (НЕ делать вслепую):
- **Экран 5 (ProcessPremium «Всё в одних руках»)** — юзер: «поменять». Уже переделывали 2×. **Спросить ЧТО именно** (другой layout / темнее / без интерактива / референс).
- **Экран 6 (InfrastructureSlider)** — юзер: «визуал дерьмо, поменять». Тоже спросить направление.

### Можно делать (есть направление):
- **Экран 7 (Metrics, график)** — ЕДИНСТВЕННОЕ из старого списка, что НЕ сделано: цифры из буклета/дашборда (динамика цены SKYSOUL), **крупнее цифры, больше график, добавить «волны СКАЙГРУПП»** (волнистый SVG-мотив). Сейчас в `SERIES` — выдуманные числа; заменить на дашборд SKYSOUL (см. §3).
- **Экран 9 (Галерея)** — ✅ СДЕЛАНО (батч 6, `GalleryFullscreen`).

### Блокеры — БОЛЬШИНСТВО ЗАКРЫТО (батч 6):
1. **Новости (12)** — ✅ ссылка на блог Тильды получена и вшита (кнопка + карточки). Скрейпить Тильду нельзя (JS-рендер). Если нужны реальные заголовки — юзер пришлёт текстом, тогда заменить заглушки в `NEWS`.
2. **Картинки наград** — ✅ получены, вшиты в блок 5 (`OurAwards`).
3. **Цифры САНТЕРРА/ОЛИМПИЯ/МЕДДИРТ** — ✅ получены текстом от юзера, внесены в `OBJECTS[].stats`.
4. **Видео стройки (1 экран)** — ✅ получено, перекодировано и вшито (батч 6).

### Ответы юзера (зафиксировано):
- Бренд — **оставляем СКАЙГРУПП** (кириллица).
- **Без** отдельного инвест-блока SKYSOUL — просто обновить цену (сделано).
- Корпоративные цифры — обновлять из буклета (сделано).

---

## 6. ⚙️ ТЕХНИЧЕСКИЕ ЗАМЕТКИ / ГРАБЛИ

- **Пути к ассетам — относительные** (`assets/...`). Не ставить `/assets/` (сломает GitHub Pages под-путь). Дев работает (рут), Pages работает (под-путь).
- **PDF от юзера НЕ отдают текст** (`pdftotext` теряет кириллицу — шрифты без ToUnicode). Читать ТОЛЬКО рендером:
  - Способ, который сработал: скопировать PDF в `public/`, поднять vite (порт 5275), открыть в **реальном Chrome через Claude-in-Chrome MCP** на `http://localhost:5275/_file.pdf`, скриншотить. (`get_page_text` тоже не даёт — графика.) Навигация по тяжёлому 58МБ-PDF морозит рендерер; кликать миниатюры / ждать после фриза и скриншотить заново. **Тяжёлые PDF из `public/` потом УДАЛИТЬ** (58 МБ раздувает сборку).
  - Буклет реально **12 страниц** (Read-тул ошибочно показал 598).
- **Скриншот-тул превью (preview_screenshot) НЕСТАБИЛЕН** с Lenis: часто таймаутит или показывает позицию с отставанием на ~секцию (Lenis перехватывает scroll). **Проверять изменения замером DOM** (`preview_eval`: `elementFromPoint`, `scrollWidth>clientWidth` для клиппинга, читать `textContent`), не полагаться только на скрин. Для рендера/скринов надёжнее Claude-in-Chrome MCP.
- **Лимит ширины цифр:** длинные числа («397 000», «324 000+») режутся в узких колонках. Правило: 2 колонки до `lg`, потом 4; либо явные размеры шрифта под колонку. Всегда проверять `scrollWidth>clientWidth` на 375px.
- **Видео-ассеты** (`public/assets/`): hero.mp4 (46МБ), about-building.mp4 (~51МБ all-intra, scroll-scrub в AboutAdvantages). Бэкапы старых медиа — в `_media-backups/` (вне сборки). ffmpeg: `C:\Users\qwert\AppData\Local\Microsoft\WinGet\Packages\Gyan.FFmpeg...\bin\ffmpeg.exe`.
- **МЁРТВЫЙ КОД** (после удаления «черточек» батчем 5): компоненты `FloatingPaths` (импорт), `BlueprintBg`, `DraftShape`, `DrawnLinesBackdrop`, файл `src/components/site/FloatingPaths.tsx`, картинка `public/assets/generated/project-sketch.jpg` — **больше не используются**, можно удалить (сейчас не мешают, esbuild не ругается). НЕ возвращать черточки — юзер их отверг.
- **Файл с правками `Правки по сайту СКАЙГРУПП.rtf` был ПУСТОЙ** (7 байт). Реальные правки юзер прислал текстом в чат (см. §7). На будущее: просить правки как **.docx / Google-Док / текст в чат** (RTF/PDF приходят без текстового слоя).
- Python в системе нет (только Store-заглушка). Скилл ui-ux-pro-max требует Python — не запускается; полагаться на свой UX/UI-опыт.

---

## 7. 📋 ПОЛНЫЙ ОРИГИНАЛЬНЫЙ СПИСОК ПРАВОК (от юзера, для сверки)

```
Хедер: кнопка связаться полупрозрачной ✅

1 экран: кнопку «Смотреть проекты» полупрозрачной ✅. Рендер стройки заменить на
реальную стройку (видео — юзер сам). Текст СКАЙГРУПП ДЕВЕЛОПМЕНТ / «Создаём знаковые
проекты Крыма — премиум, бизнес и элит-класса недвижимость у моря…» ✅

2 экран: полностью не нравится, скучный. ПОЛНАЯ ПЕРЕДЕЛКА, красивый рендер на фоне ✅.
Текст «Девелопер будущего для Крыма / СКАЙГРУПП соединяет землю…». Цифры по флаеру ✅

3 экран: визуал норм, цифры все проверить, описания: СКАЙ переделать, все объекты
переделать текст ✅ (сантерра/олимпия/меддирт цифры — флаг)

4 экран: «крым будущего» → «Полный цикл девелопмента» ✅. Карточки: 1 генподряд,
2 отельный оператор, 3 УК, 4 санатории/бальнео; 5-6 убрать ✅

5 экран: поменять ⏳ (нужно направление)
6 экран: поменять, визуал дерьмо ⏳ (нужно направление)

7 экран: цифры из буклета, крупнее цифры, больше график, мб волны скайгрупп ⏳

8 экран: карта норм ✅

9 экран: галерея не нравится, на полный экран чтобы переключалось, лучшие рендеры ⏳

10 экран: УБРАТЬ черточки ✅, фон серым (продолжение фото) ✅, текст белый ✅,
«будущим» не нравится ✅ (заменено), цифры флагманских проектов из буклета ✅

11 экран: чуть побольше людей ✅

12 экран: убрать анимацию черточек ✅, добавить картинки наград ⏳ (нужны файлы),
визуал норм — только картинки

13 экран: перенести все новости с тильды ⏳ (нужна ссылка), текст «Новости СКАЙГРУПП
девелопмент» ✅

14 экран: текст про «начните историю вашего наследия», масштабно ✅

подвал норм ✅

Ответы: 2)СКАЙГРУПП оставляем  3)без инвест-блока, обновить цену skysoul  4)цифры обновлять
```

---

## 8. Первые шаги в новом чате
1. Прочитать этот файл + `PROJECT-STATE.md`.
2. Спросить у юзера: **направление по экранам 5 и 6**, **ссылку Тильды** (новости), **картинки наград**, **цифры Сантерра/Олимпия/Меддирт**.
3. Параллельно делать экраны **7 (график+волны)** и **9 (галерея на весь экран)** — есть направление.
4. Проверять всё **замером DOM** (скрин-тул нестабилен), на 375px и десктопе.
5. Когда юзер скажет — **редеплой** (нужен новый PAT).
